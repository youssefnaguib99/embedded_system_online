/*
 ============================================================================
 Name        : Assignemt1_C_Basic_exercises.c
 Author      : youssef mohamed said

 ============================================================================
 */
//***********************************EX_1********************************
/*
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	setvbuf(stdout, NULL, _IONBF, 0);
	setvbuf(stderr, NULL, _IONBF, 0);
	int x;
	printf("enter the integer you want to check : ");
	scanf("%d",&x);
	fflush(stdin);fflush(stdout);
	if (x%2==0)
	{
		printf("%d is even",x);
		fflush(stdout);
	}
	else
	{
		printf("%d is odd",x);
		fflush(stdout);
	}
}*/

